#!/usr/bin/env bash
# set -euo pipefail

# Open hosts file in the default text editor

FILE_TO_OPEN="/etc/hosts"
FILE_TITLE="hosts configuration file"

source ./.edit_config.sh
_edit_config "$FILE_TO_OPEN" "$FILE_TITLE"
